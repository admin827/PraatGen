# PRAAT COMMANDS: Spectrum
# Source: Praat C API (praatlib.h), June 2025
# Additional commands: Session-verified via Paste Commands (Feb 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
# Sandbox arity check: Praat 6.6.30 (Linux x64v3), 29 July 2026.
#   24 documented commands probed against a live Praat install by invoking each
#   with excess arguments; Praat's "requires only N arguments" error reports the
#   true arity, compared against what this file documents.
#   Result: 11 exact arity match, 13 present and accepting the documented
#   argument count, 0 requiring a co-selection or class-specific selection
#   this harness could not supply (spot-checked, not exhaustively resolved).
#   SCOPE — this establishes that each command EXISTS on this object type and
#   that its documented PARAMETER COUNT is right. It does NOT verify parameter
#   ORDER, parameter TYPES, default values, or semantic correctness. Those
#   remain as previously sourced.

---

  # Query (session-verified — spectral moments)
  Get centre of gravity: power
  # Verified: Get centre of gravity: 2
  Get standard deviation: power
  # Verified: Get standard deviation: 2
  Get skewness: power
  # Verified: Get skewness: 2
  Get kurtosis: power
  # Verified: Get kurtosis: 2

  # Modify
  Formula: formula$

  # Convert / Analyze
  To Sound
  To Sound (resampled): interpolation depth
  To Excitation: frequency resolution
  To SpectrumTier (peaks)
  To Formant (peaks): maximum number of formants
  To Ltas: bandwidth
  To Ltas (1-to-1)
  To Spectrogram
  To Cepstrum
  To PowerCepstrum
  To Matrix

  # Draw
  Draw: from frequency, to frequency, minimum power, maximum power, garnish?
  Draw (log freq): from frequency, to frequency, minimum power, maximum power, garnish?

  # Other
  Tabulate: include bin number?, include frequency?, include real part?, include imaginary part?, include energy density?, include power density?
  Tabulate (verbose)
  Filter (pass Hann band): from frequency, to frequency, smoothing
  Filter (stop Hann band): from frequency, to frequency, smoothing
  Cepstral smoothing: bandwidth
  LPC smoothing: number of peaks, pre emphasis from

---

*End of Spectrum commands.*
