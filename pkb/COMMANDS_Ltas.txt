# PRAAT COMMANDS: Ltas
# Source: Praat C API (praatlib.h), June 2025
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
#
# Format: CommandName: param1, param2 (if arguments)
#         CommandName (no colon, if no arguments)
# Parameter notation: $ = string, ? = boolean, others = numeric
#
# Cross-type dependencies: None
# Sandbox arity check: Praat 6.6.30 (Linux x64v3), 29 July 2026.
#   10 documented commands probed against a live Praat install by invoking each
#   with excess arguments; Praat's "requires only N arguments" error reports the
#   true arity, compared against what this file documents.
#   Result: 3 exact arity match, 7 present and accepting the documented
#   argument count, 0 requiring a co-selection or class-specific selection
#   this harness could not supply (spot-checked, not exhaustively resolved).
#   SCOPE — this establishes that each command EXISTS on this object type and
#   that its documented PARAMETER COUNT is right. It does NOT verify parameter
#   ORDER, parameter TYPES, default values, or semantic correctness. Those
#   remain as previously sourced.

---

  # Query (session-verified)
  Get slope: low band floor, low band ceiling, high band floor, high band ceiling, averaging method$
  # Verified: Get slope: 0, 1000, 1000, 10000, "energy"
  # Provenance: Phonanium AVQI v02.06 script; Paste Commands 24 Feb 2026
  #   Get number of bins
  # Verified: Get number of bins (no arguments)
  # Provenance: PRAAT_DEFINITIVE_CATALOGUE.txt, 26 Mar 2026

  Get frequency from bin number: binNumber
  # Verified: Get frequency from bin number: 1
  # Provenance: PRAAT_DEFINITIVE_CATALOGUE.txt, 26 Mar 2026

  Get value in bin: binNumber
  # Verified: Get value in bin: 100
  # Provenance: PRAAT_DEFINITIVE_CATALOGUE.txt, 26 Mar 2026


  # Modify
  Formula: formula$
  # Convert / Analyze
  To SpectrumTier (peaks)
  To Matrix
  # Draw
  Draw: from frequency, to frequency, from power, to power, garnish?, drawing method$
  # Other
  Report spectral trend: from frequency, to frequency, frequency scale$, fit method$
  Compute trend line: from frequency, to frequency
  Subtract trend line: from frequency, to frequency

---

*End of Ltas commands.*
