# PRAAT COMMANDS: Strings
# Source: Praat C API (praatlib.h), June 2025
# Additional commands: Session-verified via Paste Commands (Feb 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
# Sandbox arity check: Praat 6.6.30 (Linux x64v3), 29 July 2026.
#   21 documented commands probed against a live Praat install by invoking each
#   with excess arguments; Praat's "requires only N arguments" error reports the
#   true arity, compared against what this file documents.
#   Result: 8 exact arity match, 13 present and accepting the documented
#   argument count, 0 requiring a co-selection or class-specific selection
#   this harness could not supply (spot-checked, not exhaustively resolved).
#   SCOPE — this establishes that each command EXISTS on this object type and
#   that its documented PARAMETER COUNT is right. It does NOT verify parameter
#   ORDER, parameter TYPES, default values, or semantic correctness. Those
#   remain as previously sourced.

---

  # Create / Read
  Create Strings from texts: name$
  Create Strings as file list: name$, path$
  Create Strings as folder list: name$, path$
  Create Strings from tokens: name$, text$, separators$
  Read Strings from raw text file: file name$

  # Query (API)
  Get string: position
  List all strings

  # Query (session-verified)
  Get number of strings
  # Verified: Get number of strings

  # Modify
  Set string: position, new string$
  Insert string: at position, string$
  Remove string: position
  Replace all: find$, replace with$, replace limit per string, find and replace strings are$

  # Convert / Analyze
  To Distributions
  To Permutation: sorting method$
  To WordList

  # Extract / Combine
  Extract part: from index, to index

  # Other
  Save as raw text file: file name$
  Equal?
  Randomize
  Sort
  Convert to backslash trigraphs
  Convert to Unicode

---

*End of Strings commands.*
