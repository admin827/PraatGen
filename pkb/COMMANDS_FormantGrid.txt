# PRAAT COMMANDS: FormantGrid
# Source: PRAAT_VERIFIED_COMMANDS whitelist (Paste Commands, February 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
#
# Format: CommandName: param1, param2 (if arguments)
#         CommandName (no colon, if no arguments)
# Parameter notation: $ = string, ? = boolean, others = numeric
#
# Cross-type dependencies: Filter and Filter (no scale) require
# Sound + FormantGrid selected simultaneously.
# Sandbox arity check: Praat 6.6.30 (Linux x64v3), 29 July 2026.
#   7 documented commands probed against a live Praat install by invoking each
#   with excess arguments; Praat's "requires only N arguments" error reports the
#   true arity, compared against what this file documents.
#   Result: 0 exact arity match, 5 present and accepting the documented
#   argument count, 2 requiring a co-selection or class-specific selection
#   this harness could not supply (spot-checked, not exhaustively resolved).
#   SCOPE — this establishes that each command EXISTS on this object type and
#   that its documented PARAMETER COUNT is right. It does NOT verify parameter
#   ORDER, parameter TYPES, default values, or semantic correctness. Those
#   remain as previously sourced.

---

  # Create
  Create FormantGrid: name$, start time, end time, number of formants, initial first formant, initial formant spacing, initial first bandwidth, initial bandwidth spacing

  # Modify — formant points
  Remove formant points between: formant number, from time, to time
  Add formant point: formant number, time, frequency

  # Modify — bandwidth points
  Remove bandwidth points between: formant number, from time, to time
  Add bandwidth point: formant number, time, bandwidth

  # Cross-type operations (require Sound + FormantGrid selected)
  Filter
  Filter (no scale)

---

*End of FormantGrid commands.*
