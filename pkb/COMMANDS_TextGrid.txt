# PRAAT COMMANDS: TextGrid
# Source: Praat C API (praatlib.h), June 2025
# Additional commands: Session-verified via Paste Commands (Feb 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
# Sandbox-verified: Praat 6.6.30 (Linux x64v3), 29 July 2026 — `Scale times` arity corrected (0 -> 2); Pitch/Sound co-selection requirement documented for the Draw/Speckle/Extract blocks

---

  # Create / Read
  Create TextGrid: start time, end time, all tier names$, which of these are point tiers$
  Read TextGrid from Xwaves: sound file path$, tiers are point tiers?, override number of tiers, file name$

  # Query (API)
  List: include line number?, time decimals, include tier names?, include empty intervals?
  Get tier name: tier number
  Get label of interval: tier number, interval number
  Get label of point: tier number, point number
  Get starting points: tier number, get starting points whose label$, the text$
  Get end points: tier number, get end points whose label$, the text$
  Get centre points: tier number, get centre points whose label$, the text$
  Get points: tier number, get points whose label$, the text$
  Get points (preceded): tier number, get points whose label$, the text$, preceded by a label that$, the text$
  Get points (followed): tier number, get points whose label$, the text$, followed by a label that$, the text$

  # Query — interval tier (session-verified)
  Get number of intervals: tier number
  # Verified: Get number of intervals: 1
  Get interval at time: tier number, time
  # Verified: Get interval at time: 1, 0.5
  Get start time of interval: tier number, interval number
  # Verified: Get start time of interval: 1, 1
  Get end time of interval: tier number, interval number
  # Verified: Get end time of interval: 1, 1

  # Query — point tier (session-verified)
  Get number of points: tier number
  # Verified: Get number of points: 1
  Get time of point: tier number, point number
  # Verified: Get time of point: 1, 1

  # Modify
  Insert interval tier: position, name$
  Insert point tier: position, name$
  Remove tier: tier number
  Set tier name: tier n umber, name$
  Insert boundary: tier number, time
  Remove left boundary: tier number, interval number
  Remove right boundary: tier number, interval number
  Remove boundary at time: tier number, time
  Set interval text: tier number, interval number, text$
  Replace interval texts: tier number, from interval, to interval, search string$, replace string$, search type$
  Insert point: tier number, time, text$
  Remove point: tier number, point number
  Remove points: tier number, remove every point whose label$, the text$
  Set point text: tier number, point number, text$
  Replace point texts: tier number, from interval, to interval, search string$, replace string$, search type$d

  # NOTE important label-handling behavior when inserting/removing interval boundaries. If a longer boundary with a label is bisected by a new boundary, the existing label goes to the left most interval. The 'new' interval to the right will be empty. If a boundary is deleted between two labeled intervals, those labels concatenate in the new, larger interval. 

  # Convert / Analyze
  Down to Table: include line number?, time decimals, include tier names?, include empty intervals?
  To DurationTier: tier number, time scale factor, left transition duration, right transition duration, scale intervals whose label$, the text$

  # Extract / Combine
  Append
  Extract all intervals: tier number, preserve times?
  Extract non-empty intervals: tier number, preserve times?
  Extract intervals where: tier number, preserve times?, extract every interval whose label$, the text$
  Extract one tier: tier number
  Extract part: from time, to time, preserve times?
  Merge: equalize domains?
  Concatenate

  # Draw
  # ⚠️ CO-SELECTION REQUIRED (sandbox-verified, Praat 6.6.30, 29 Jul 2026).
  #   Every command in this Draw/Speckle block operates on a Pitch object
  #   TOGETHER WITH a TextGrid, and both must be selected:
  #       selectObject: pitchId, textGridId
  #   With a TextGrid alone these fail. `Draw:` on a bare TextGrid is a
  #   DIFFERENT command taking only 5 arguments (from time, to time, show
  #   boundaries?, use text styles?, garnish?) — passing the 9 below to a
  #   lone TextGrid gives "Command requires only 5 arguments, not the 9 given".
  #   The same applies to `Extract all intervals`, `Extract non-empty
  #   intervals` and `Extract intervals where`, which need a Sound co-selected.
  Draw: tier number, from time, to time, from frequency, to frequency, font size, use text styles?, text alignment$, garnish?
  Draw logarithmic: tier number, from time, to time, from frequency, to frequency, font size, use text styles?, text alignment$, garnish?
  Draw semitones: tier number, from time, to time, from frequency, to frequency, font size, use text styles?, text alignment$, garnish?
  Draw mel: tier number, from time, to time, from frequency, to frequency, font size, use text styles?, text alignment$, garnish?
  Draw erb: tier number, from time, to time, from frequency, to frequency, font size, use text styles?, text alignment$, garnish?
  Speckle: tier number, from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Speckle logarithmic: tier number, from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Speckle semitones: tier number, from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Speckle mel: tier number, from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Speckle erb: tier number, from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw separately: from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw separately (logarithmic): from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw separately (semitones): from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw separately (mel): from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw separately (erb): from time, to time, from frequency, to frequency, show boundaries?, use text styles?, garnish?
  Draw: from time, to time, show boundaries?, use text styles?, garnish?
  Draw with Sound?
  Draw with Pitch?

  # Other
  View & Edit
  Scale times: new start time, new end time
  # Verified: Scale times: 0, 2   (sandbox, Praat 6.6.30, 29 Jul 2026)
  # CORRECTION (29 Jul 2026): previously documented with no arguments. The bare
  # form fails with "Command Scale times not available for current selection".
  Align interval: tier number, interval number, language$, include words?, include phonemes?
  Clone time domain
  Save as chronological text file: file name$
  View & Edit alone
  View & Edit with Sound?
  Tabulate occurrences: search tiers#, list every label that$, the text$, case sensitive?
  # Verified: Tabulate occurrences: 1, "is equal to", "a", "no"
  # `search tiers#` is a REALVECTOR of 1-based tier indices; default is
  # the single-tier case `1` which the autocomplete renderer expands to
  # the vector literal `{1}`. Multi-tier example: `{1, 2}`.
  # `list every label that$` (OPTIONMENU_ENUM): canonical "is equal to";
  #   other options: "contains", "starts with", "ends with",
  #   "matches (regex)", "is not equal to", "does not contain",
  #   "does not start with", "does not end with", "does not match (regex)".
  # The earlier 3-arg signature dropped the leading search-tiers vector.
  # Provenance: Empirical verification, Praat 6.4.65, 17 May 2026.
  Is interval tier: tier number
  Convert to backslash trigraphs
  Convert to Unicode
  Extend time: extend domain by, position$
  Duplicate tier: tier number, position, name$

---

*End of TextGrid commands.*
